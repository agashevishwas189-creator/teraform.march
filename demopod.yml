apiVersion: v1
kind: Pod
metadata:
    name: demo-pod
    labels: 
      app: demo-pod
spec:
  containers:
    - name: y-container
      image: httpd:latest
      ports: 
        - containerPort: 80
     